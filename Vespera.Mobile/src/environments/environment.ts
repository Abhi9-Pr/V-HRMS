export const environment = {
  production: false,
  // Android emulator's loopback alias for the host machine; a physical device needs the host's
  // LAN IP instead (Capacitor's dev-server story is a later phase's concern, see AGENTS.md).
  apiBaseUrl: 'https://10.0.2.2:7095',
};
